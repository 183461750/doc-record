{
  "name": "doc-record",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "generate-sidebar": "python generate_sidebar.py",
    "serve": "npm run generate-sidebar && docsify serve docs"
  },
  "dependencies": {
    "docsify-cli": "^4.4.4"
  }
}
